[project]
name = "roque-cmap"
version = "0.1.0"
description = ""
authors = [
    {name = "Jarred Green", email = "astrojarred@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
dev = [
    "numpy>=1.24.2",
    "seaborn>=0.12.2",
    "matplotlib>=3.7.0",
    "pytest>=8.3.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatchling.build.targets.wheel]
packages = ["roque_cmap"]
